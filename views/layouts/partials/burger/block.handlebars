<li class="list-group-item burger-item">
    {{burger_name}}

    <button class="btn btn-outline-success devour-button" data-id="{{id}}" data-devour="{{devour}}">
        {{#if devour}}DEVOUR!{{else}}DELETE!{{/if}}
    </button>
</li>